import {{componentName}} from './{{componentName}}';

describe('{{componentName}}', () => {
  it('renders', () => {
    const wrapper = shallow(<{{componentName}} />);

    expect(wrapper).toHaveClassName('divStyle');
  });
});
